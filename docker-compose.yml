version: "3.9"

services:

  backend:
    build: ./backend
    ports:
      - "5000:5000"

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: project_db

    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: